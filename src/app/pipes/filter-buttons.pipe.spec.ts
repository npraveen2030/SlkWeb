import { FilterButtonsPipe } from './filter-buttons.pipe';

describe('FilterButtonsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterButtonsPipe();
    expect(pipe).toBeTruthy();
  });
});
